fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Starlette 1.0.0 は Jinja2 TemplateResponse で "TypeError: cannot use 'tuple' as a dict key" が出る既知バグがあるため 1.0.0未満に固定する
# (https://github.com/pallets/jinja/issues/2180)
starlette<1.0.0

Pillow>=10.0.0
requests>=2.31.0

enka>=2.0.0
